// Empty in Phase 0. Once features/* start growing a real API surface
// (Phase 1+), this package generates its types from the backend's OpenAPI
// schema (`apps/api` exposes it at /openapi.json) rather than hand-writing
// duplicate interfaces on the frontend — see plan.md §2.
export {};
